# AI秘書 v2.4 引き継ぎメモ

## 正本

```text
C:\Users\USER\OneDrive\ドキュメント\AI研究所作り\ai_secretary_v01
```

このフォルダのみを正本として扱う。

## 現在バージョン

```text
AI秘書 v2.4
```

## 技術構成

- Python
- Flask
- SQLite
- PWA最小対応
- iPhone同一Wi-Fiアクセス対応

## 起動方法

```powershell
python app.py
```

## アクセス

PC:

```text
http://127.0.0.1:5000
```

iPhone:

```text
http://192.168.11.5:5000
```

## DB

```text
secretary.db
```

主なテーブル:

- secretary_tasks
- secretary_schedule
- secretary_transcripts
- secretary_contacts

## 現在できていること

- テキスト入力
- ToDo抽出
- 期限抽出
- 優先順位A/B/C
- タスク保存
- タスク完了
- タスク削除
- 今日の重要タスク表示
- 期限超過/今日までタスク表示
- 音声入力
- カメラ記録入口
- 名刺OCR入口
- 録音メモ
- 録音保存
- 文字起こし
- 文字起こし履歴保存
- 会話からToDo候補抽出
- 整理結果のToDo保存
- 予定候補保存
- 予定一覧DB保存
- 予定削除
- 会話履歴一覧
- 会話履歴削除
- 簡易利用キー
- PWA設定
- 見やすさ設定
- 名刺連絡先候補保存
- 連絡先一覧
- 連絡先削除

## 最新追加

### v2.4 名刺DB保存

- `secretary_contacts` テーブル追加
- 連絡先候補として保存ボタン追加
- 連絡先一覧追加
- 削除ボタン追加

## 注意点

OCR本体はまだ未実装。

現在は以下まで:

- 名刺画像選択
- プレビュー
- OCR準備中表示
- 連絡先候補の手入力保存

## 触る正本

```text
ai_secretary_v01
```

## 触らないもの

- パイソンAI.py
- bot.py
- 研究所Bot

## 作業後チェック

作業後は必ず以下を実行する。

1. 保存
2. `python -m py_compile app.py`
3. `python app.py` 起動確認
4. `http://127.0.0.1:5000` 表示確認
5. 必要なら iPhone で `http://192.168.11.5:5000` 確認

## 次におすすめ

- 次候補未定

## v2.5 追加

### 連絡先検索

- 連絡先一覧の上に検索欄を追加
- 氏名、会社名、電話番号、メールアドレスを対象に部分一致検索
- 入力と同時に絞り込み
- 該当件数表示
- 検索クリアボタン追加

## v2.6 追加

### 名刺OCR本体

- pytesseract と Pillow を使用
- 名刺画像から文字抽出
- 氏名、会社名、電話番号、メールアドレスを候補推定
- OCR結果を表示
- 連絡先候補欄へ自動入力
- Tesseract OCR 未導入時は案内表示

## v2.7 追加

### 予定通知強化

- ホーム上部に「近い予定」エリアを追加
- 現在以降の予定を近い順で最大5件表示
- 24時間以内の予定を強調表示
- 件数表示を追加
- 予定一覧への移動リンクを追加

## v2.8 追加

### iPhone実機調整

- 画面下部の余白を追加
- 入力欄のiPhoneズーム/表示崩れ対策
- 録音ボタン周辺の押しやすさを改善
- 名刺OCRエリアのボタン配置と余白を調整
- 連絡先検索欄のスマホ表示を調整
- 予定一覧の文字サイズと折り返しを調整
- 会話履歴の折り返しを調整
- PWA追加案内の見やすさを改善
- 320px/375px/390px幅の横スクロール対策

## v3.0 追加

### AI秘書モード

- ホーム上部に「今日の秘書レポート」を追加
- 近い予定 上位3件を表示
- 期限が近いタスク 上位3件を表示
- 最近の会話 最新3件を表示
- 最近の連絡先 最新3件を表示
- 既存ToDoの期限、優先度、未完了状態から「今日やること」を簡易生成
- AI APIなし、ルールベース
- DB構造変更なし

## v3.1 追加

### AI要約

- 「要約する」ボタンを追加
- 最新の文字起こし結果を要約可能
- 会話履歴ごとに要約可能
- `📄 要約結果` 表示エリアを追加
- 最初の3文、ToDo候補、予定候補でルールベース要約
- AI APIなし
- DB構造変更なし

## v3.2 追加

### AI提案

- 今日の秘書レポートに「秘書からの提案」を追加
- 期限が近いタスクから優先提案
- 24時間以内の予定から事前準備提案
- 未完了タスク件数から優先順位提案
- 最近の会話のToDo候補から確認提案
- 最大3件表示
- AI APIなし、ルールベース
- DB構造変更なし

## v3.3 追加

### AI秘書に相談

- ホームに「秘書に相談」エリアを追加
- 質問入力欄と相談ボタンを追加
- 未完了タスク、期限付きタスク、近い予定、最近の会話、秘書からの提案を参照
- ルールベースで回答
- 回答履歴を画面内に最新5件まで表示
- AI APIなし
- DB構造変更なし

## 次回作業メモ

機能が増えてきたため、次の大きな作業に入る前にこの v2.4 状態を基準点として扱う。

特に次に進む候補は以下:

- OCR本体
- 連絡先検索
- 予定通知
- iPhone実機調整

上記へ進む場合も、作業対象は `ai_secretary_v01` のみに限定する。
